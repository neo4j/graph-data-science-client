# Fixed console width shared across the CLI so rules, tables, and dividers
# render at a consistent, readable line length in both `gds database` and
# `gds session` output instead of stretching across a wide terminal.
CONSOLE_WIDTH = 80
