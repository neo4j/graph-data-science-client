# Closeness centrality on the social graph, run twice with different parameter
# sets (standard vs Wasserman-Faust). Also a runnable config.
# graphs: social
session:
  memory: 2GB
  ttl: 30m
jobs:
- project:
    type: cypher
    query: |
      MATCH (source:Person)-[rel:KNOWS]->(target:Person)
      RETURN gds.graph.project.remote(source, target, {
        sourceNodeLabels: ['Person'],
        targetNodeLabels: ['Person'],
        relationshipType: type(rel)
      })
  compute:
  - compute: closeness
    config:
      resultProperty: closeness
  - compute: closeness
    config:
      resultProperty: closenessWf
      useWassermanFaust: true
  mutate:
  - nodeProperty: closeness
  - nodeProperty: closenessWf
  write:
  - nodeProperty: closeness
  - nodeProperty: closenessWf
