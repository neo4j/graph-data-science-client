# Betweenness centrality on the social graph, run twice with different parameter
# sets (exact vs sampled). Also a runnable config.
# graphs: social
session:
  memory: 2GB
  ttl: 30m
jobs:
- project:
    type: cypher
    query: |
      MATCH (source:Person)-[rel:KNOWS]->(target:Person)
      RETURN gds.graph.project.remote(source, target, {
        sourceNodeLabels: ['Person'],
        targetNodeLabels: ['Person'],
        relationshipType: type(rel)
      })
  compute:
  - compute: betweenness
    config:
      resultProperty: betweenness
  - compute: betweenness
    config:
      resultProperty: betweennessSampled
      samplingSize: 3
      samplingSeed: 42
  mutate:
  - nodeProperty: betweenness
  - nodeProperty: betweennessSampled
  write:
  - nodeProperty: betweenness
  - nodeProperty: betweennessSampled
